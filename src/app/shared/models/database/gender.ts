export class Gender {
    genderId: number;
    genderValue: string;

    constructor(genderId: number, genderValue: string) {
        this.genderId = genderId;
        this.genderValue = genderValue;
    }
}
